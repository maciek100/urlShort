services:

  petiteurl-service:
    build: .
    container_name: petiteurl-service
    ports:
      - "8080:8080"
      - "5005:5005"
    environment:
      MONGODB_URI: "mongodb+srv://maciek100:Wisla@cowboy.r44csn2.mongodb.net/Cowboy?retryWrites=true&w=majority&appName=Cowboy"


    networks:
      - petiteurl-network

  petiteurl-frontend:
    build:
      context: ./petiteurl-frontend
    ports:
      - "8081:80"
    depends_on:
      - petiteurl-service

  petiteurl-client:
    build: ../petiteClient
    depends_on:
      - petiteurl-service
    networks:
      - petiteurl-network

  memcached:
    image: memcached:latest
    container_name: memcached
    ports:
      - "11211:11211"
    networks:
      - petiteurl-network

networks:
  petiteurl-network:
    driver: bridge